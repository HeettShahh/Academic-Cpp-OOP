### 1. Intro Games & Structs
* **Focus:** Logic Building & Intro to OOP
* **Description:** A collection of classic programming challenges and a custom mathematical structure, marking the beginning of my C++ coursework.
* **Key Components:**
    * [cite_start]**Fraction Struct (`problem4.cpp`):** A precursor to my Matrix library; defines a `Fraction` struct with overloaded operators (`+`, `-`, `*`, `/`) to handle arithmetic logic manually[cite: 173].
    * [cite_start]**Interactive Games (`problem1.cpp`, `problem3.cpp`):** Implements "Rock-Paper-Scissors" and a "Number Guessing Game" using `rand()` generation and conditional loops[cite: 173].
    * [cite_start]**FizzBuzz (`problem2.cpp`):** Solves the classic interview problem using modular arithmetic and input validation[cite: 173].
* **Usage:**
    ```bash
    cd 01_Intro_CPP_Games_Structs
    clang++ problem4.cpp -o fraction_calculator
    ./fraction_calculator
    ```
